<h1>Find Friends</h1>

     <% @users.each do |user| %>
     <tr>
     	<td><%= user.uname %></td>
     	<%= link_to "Add friend",friendships_path(:friend_id) %>
     </tr>
     <% end %>

     
    
